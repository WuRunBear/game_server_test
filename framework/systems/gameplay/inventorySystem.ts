import type { GameWorld } from "src/world";

/**
 * 背包系统（占位）：用于后续处理物品增删、容量、堆叠等逻辑。
 *
 * @param world ECS World
 * @returns 处理后的 World
 */
export function inventorySystem(world: GameWorld): GameWorld {
  return world;
}
