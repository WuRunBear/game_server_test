## 项目工具与运行环境

- 项目路径：g:\wrbtest\game
- 运行环境：Windows；Node.js >= 22（ESM：type=module）
- 包管理器：pnpm
- 工具链：TypeScript（typescript ^5.8，@tsconfig/node22），tsx
- 常用命令：pnpm dev（tsx watch src/index.ts）；pnpm build（tsc -p tsconfig.json）；pnpm start（node dist/index.js）
- 主要依赖：ws、bitecs
